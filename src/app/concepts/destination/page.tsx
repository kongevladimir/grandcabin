import type { Metadata } from "next";
import { DestinationConcept } from "@/components/ConceptPages";

export const metadata: Metadata = { title: "Destinasjon · Grandcabin" };

export default function DestinationPage() {
  return <DestinationConcept />;
}
